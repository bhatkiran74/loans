server:
  port: 8092
spring:
  application:
    name: "loan"
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/loan
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update